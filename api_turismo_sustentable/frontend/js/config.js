// config.js — Cambiá API_URL por la URL de Render en producción
const CONFIG = {
  // Desarrollo local
  API_URL: "http://localhost:8000",
 
  // Producción en Render:
  // API_URL: "https://latam-tourism-api.onrender.com",
};