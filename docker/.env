# ─── Airflow ──────────────────────────────────────────────────────────────────
# Generar con: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AIRFLOW_FERNET_KEY=uhh55a9pfeKRJcen5bo94zDR6L5xvTDdS10nsZxcRJs=

# Generar con: openssl rand -hex 32
AIRFLOW_SECRET_KEY=647822acfa96ede2f10e446ed75722a8ac3e097d8584412400ee8837ccfa0388

AIRFLOW_ADMIN_USER=admin
AIRFLOW_ADMIN_PASSWORD=cambiar_en_produccion

# ─── Proyecto ─────────────────────────────────────────────────────────────────
S3_BUCKET=latam-sustainability-datalake

# ─── AWS (NO necesario en EC2 con IAM Role) ───────────────────────────────────
# En EC2 con IAM Role, boto3 toma credenciales automáticamente.
# Solo descomentar si corrés en local:
# AWS_ACCESS_KEY_ID=  
# AWS_SECRET_ACCESS_KEY= 
# AWS_DEFAULT_REGION=us-east-1

AIRFLOW__SMTP__SMTP_HOST=smtp.gmail.com
AIRFLOW__SMTP__SMTP_USER=tu_email@gmail.com
AIRFLOW__SMTP__SMTP_PASSWORD=tbrg caew pyzs gcko
AIRFLOW__SMTP__SMTP_PORT=587
AIRFLOW__SMTP__SMTP_MAIL_FROM=tu_email@gmail.com